/** ISO date of the current fix. */
export const FIX_DATE_ISO = '2026-04-18';

/** UTC time of the nominal strike. */
export const FIX_TIME_UTC = '00:30';

/** Full ISO datetime of the nominal strike. */
export const STRIKE_AT = `${FIX_DATE_ISO}T${FIX_TIME_UTC}:00Z`;
