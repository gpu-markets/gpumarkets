{
  "name": "gpu-markets-site",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "The public site for GPU Markets — an open reference price index for the GPU rental market.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "bash scripts/build.sh",
    "build:raw": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0"
  }
}
